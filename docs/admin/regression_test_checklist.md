# リグレッションテスト チェックリスト

ベッドコントロールシミュレーターの主要機能を、修正後に手動で確認するためのチェックリスト。
自動化されたスモークテスト（`scripts/hooks/smoke_test.py`）と合わせて使用する。

## 前提条件
- Python 3.9+、依存関係インストール済み（streamlit, pandas, numpy, jpholiday 等）
- `streamlit run scripts/bed_control_simulator_app.py` でローカル起動

## 自動チェック（pre-commit で自動実行）

`python3 scripts/hooks/smoke_test.py` で下記を一括検証：

| # | 検証項目 | 期待値 |
|---|---|---|
| 1 | CSV 行数 | 220（110日 × 2病棟） |
| 2 | 4月 5F 稼働率 | 85.5-87.0% |
| 3 | 4月 6F 稼働率 | 89.5-91.5% |
| 4 | 4月 全体稼働率 | 87.5-89.5% |
| 5 | 5F 単体月次目標（残10日） | 96.0-99.0%（非現実的） |
| 6 | 6F 単体月次目標（残10日） | 87.0-91.0%（達成可能） |
| 7 | 均等努力 Δ | +3.0〜+7.0pt（必ず正） |
| 8 | 5F rolling LOS (90日) | 15-21日（基準内） |
| 9 | 6F rolling LOS (90日) | 17-23.5日（drifting story） |
| 10 | `bed_control_simulator_app.py` 構文チェック | OK |

## 手動チェック（UIで確認）

### A. 表示モード × 病棟選択の組み合わせ
- [ ] 実績モード × 全体 → 稼働率88.4%、Δ +4.8pt 表示
- [ ] 実績モード × 5F → 稼働率86.3%、単体目標97%表示、フェーズ構成タブで C群が0%でない
- [ ] 実績モード × 6F → 稼働率90.5%、単体目標89%表示、rolling LOS 22.6日（基準超過アラート）
- [ ] シミュレーションモード × 全体 → NameError なし
- [ ] シミュレーションモード × 5F/6F → 同上

### B. 日次推移タブ
- [ ] 4月1日〜20日のみ表示（過去90日データが混入していない）
- [ ] 単体目標線（赤/オレンジ/緑破線）と均等努力目標線（青点線）が**両方**表示
- [ ] ラベル位置が重ならず読み取れる

### C. フェーズ構成タブ
- [ ] 6F選択時、C群が0%ではなく適切な値（20人前後）
- [ ] What-if スライダー（月間入院数・目標稼働率）が病棟別では60-90人レンジ
- [ ] Little法則の計算プロセスが expander で表示

### D. 本日の病床状況 / 意思決定ダッシュボード
- [ ] 5F・6F の rolling LOS が**並列**表示
- [ ] 6Fが基準超過（22.6日 > 21日）時にアラート発動
- [ ] 施設基準判定は**各病棟ごと**で、病院全体ではない

### E. クリア計画
- [ ] 「全体」モード時、超過病棟（6F）のクリア計画のみ表示
- [ ] 「C群追加退院3名」など現実的な指示内容

### F. HOPE送信サマリー
- [ ] 5F/6F個別の rolling LOS 入力欄が表示
- [ ] 400文字制限内で生成される

### G. 均等努力メッセージ
- [ ] Δ > 0 の時：「5F→91%、6F→95.3%、均等努力で目標達成」メッセージ
- [ ] Δ ≤ 0 の時（仮想）：「既に目標達成ペース、追加努力不要」メッセージ
- [ ] 稼働率は**経営目標**、施設基準は**平均在院日数のみ**と明記

### H. 院内LAN導入前 安全対策（v3.5m / Phase 1〜1.8, 2026-05-01 追加）

#### H-1. データ純度バナー
- [ ] 「⚙️ データ・設定 > 📋 日次データ入力」タブ最上部にバナーが表示される
- [ ] CSV にデモ行（A医師〜J医師）のみ → `⚠️ デモデータ`
- [ ] CSV に実医師コード（KJJ/HAYT/OKUK 等 17 コード）のみ → `✅ 実データ`
- [ ] 両方混在 → `🔴 デモと実の混在`
- [ ] 行数 0（ヘッダーのみ） → `📭 空（未入力）`

#### H-2. 本番初期化（デモ退避）
- [ ] 「🛠 本番初期化（デモデータを退避 → 空スキーマを作成）」expander が展開できる
- [ ] チェックボックス同意 → 「📦 本番初期化を実行」ボタン押下
- [ ] `data/archive/admission_details_demo_YYYYMMDD_HHMMSS.csv` が作成される
- [ ] 実行後バナーが `📭 空（未入力）` に変わる
- [ ] `data/admission_details.csv` が空スキーマ（ヘッダーのみ）になる

#### H-3. 救急15% 手動シード反映（4箇所すべて確認）
`settings/manual_seed_emergency_ratio.yaml` の `seeds:` に 4・5 月の値（5F / 6F / all）を入れた状態で：

- [ ] **救急搬送15% 専用タブ**（`🛡️ 制度・需要・C群 > 🚑 救急搬送15%`）— `🌉 シード` ラベル付きで月別内訳表示
- [ ] **制度余力ダッシュボード**（同セクション > `🛡️ 制度余力`）— 「救急搬送後患者割合」description に `直近3ヶ月rolling平均（シード混在） XX.X%（…🌉…）` 表示
- [ ] **結論カード**（`📊 今日の運営 > 意思決定ダッシュボード`）— 救急ratio がシード月を含めて算出されている
- [ ] **v4 制度確認タブ**（v4 アプリ起動時のみ）— 同等の表示

#### H-4. 看護必要度 月次シード反映（5箇所すべて確認）
`settings/manual_seed_nursing_necessity.yaml` の `seeds:` に 4・5 月の 4 値（I_total, I_pass1, II_total, II_pass1）を入れた状態で：

- [ ] **過去1年分析 > 看護必要度トレンド** の月別出典マトリクスで `🌉 シード` 表示
- [ ] **年間カード**（同タブ）— I 値 / II 値が seed-merged データから算出
- [ ] **6F ストラテジーボード** — 今日の結論 / 月次不足 / 日次不足 / C23 間隔 / 必要ペースの数値が、シード入力前後で **変化する**
- [ ] **5F ストラテジーボード**（同等）
- [ ] シード値が null を 1 つでも含む月は採用されない（4 値全部入りで初めて反映）

#### H-5. 初期状態対応（空 detail_df + シードのみ）
- [ ] 本番初期化直後（日次 0 行）に救急シードを入力 → 制度余力ダッシュボードで `data_source="measured"` が表示される（not_available にならない）
- [ ] 同じく看護必要度シードでも 6F ストラテジーボードが空ではなく値を表示

#### H-6. シード卒業判定
- [ ] 過去 1 年 CSV（`data/past_admissions_2025fy.csv`）で代替済のシード月がある場合
- [ ] 「📈 過去1年分析」タブ上部に `🎓 bridge 卒業: …` バナー表示

## I. 表示整合性チェック（v3.5m.1 / PR #47-49, 2026-05-01）

### I-1. 上部 KPI ストリップの当日値・月平均併記
- [ ] 「⚙️ データ・設定」以外のセクションを開いた時、画面上部 KPI ストリップに 3 列表示
- [ ] 1 列目「{ward} 稼働率（直近）」 = 当日値（最新日スナップショット）
- [ ] 1 列目右側に薄いグレーで「（月平均 X.X%）」を併記
- [ ] 2 列目「今の空床」、3 列目「在院患者数」（いずれも当日値）

### I-2. 「本日の詳細サマリー」expander の月末目標明示
- [ ] expander を開くとゲージタイトルが「現時点の月平均稼働率（{ward}）／月末目標 ≥ 90%」と表示される
- [ ] 病棟比較ミニバッジに「月平均（月末目標 ≥ 90%）」キャプション + 各病棟の判定（「✅ 目標達成ペース」 or 「⚠️ ペース未達」）
- [ ] アクション分岐の文言が「月末 90% 目標 — 達成ペース」「月末 90% 目標 — ペースアップ必要」など期間とラベルが 1:1 対応で明示
- [ ] 月平均達成見通しテキストが「月末 90% 目標 — 現在の月平均 X%、残り N 日で平均 Y% 必要」形式

### I-3. 日次推移グラフのサマリー統合
- [ ] 「本日の詳細サマリー」expander 内、見出し「📈 月平均稼働率の経過（日次推移グラフ）」の下に、稼働率の推移ヒーローチャートが表示される
- [ ] 同じ内容が「📊 今日の運営 > 📊 日次推移」タブからも従来通り見られる
- [ ] expander 内とタブで同じグラフが表示される（関数化による単一ソース）

### I-4. 結論カード・上部 KPI・サマリー内ゲージの整合性
- [ ] 結論カード（緑 / 黄 / 赤）の稼働率値 = 上部 KPI ストリップの「（直近）」値 = 「今朝の病棟状況」の稼働率
- [ ] サマリー内ゲージの値 = 上部 KPI ストリップの「（月平均 X.X%）」値
- [ ] 当日値と月平均は **常に括弧やラベルで明示的に区別** されている

## 重要な不変条件（Invariants）

以下が崩れたら**必ず**リグレッション：

1. **均等努力 Δ > 0**: デモデータの核。負になるとストーリーが破綻する。
2. **5F単体目標が「困難」、6F単体目標が「達成可能」**: 単一病棟では足りず協力が必要というメッセージ。
3. **6F rolling LOS > 5F rolling LOS**: 6Fが drifting しているストーリー。
4. **施設基準 = 平均在院日数のみ**: 稼働率は経営目標であって施設基準ではない。UIメッセージで混同しない。
5. **過去90日データはrolling LOSのみに使用**: 日次推移チャートや月次集計に混入しない。

## 意図的に変更するとき

スモークテストの EXPECTED を更新する際は、必ず：
1. 変更理由をコミットメッセージに記載
2. 対応する UI/ドキュメント/シナリオ台本を連動更新
3. 新しい期待値が上記「不変条件」に反していないか確認
